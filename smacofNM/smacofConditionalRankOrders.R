# the data for smacofConditionalRankOrders are 3-tuples (i, j, k)
# coding that (i, j) is more similar than (i, k)
# this can be used for (i,j,k) from the same set
# or i from one set and (j,k) from another set (as in unfolding)
